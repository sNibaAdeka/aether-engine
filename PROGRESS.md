# AETHER — журнал фаз

Статус на 2026-07-27.

| # | Фаза | Статус |
|---|---|---|
| 0 | Фундамент и инструменты | ✅ завершена |
| 1 | Террейн: генерация и геометрия | 🟡 1a готова, 1b: тайлы и эрозия работают, швы — долг |
| 2 | Материалы террейна | 🟡 всё из ТЗ реализовано, текстуры процедурные, швы фазы 1 видны |
| 3 | Атмосфера и небо | ✅ завершена |
| 4 | Тени | ⬜ не начата |
| 5 | Объёмные облака и объёмный свет | ⬜ не начата |
| 6 | Вода | ⬜ не начата |
| 7 | Растительность | ⬜ не начата |
| 8 | Погода, время и сезоны | ⬜ не начата |
| 9 | Глобальное освещение и AO | ⬜ не начата |
| 10 | Пост-обработка | ⬜ не начата |
| 11 | Игрок, физика, звук | ⬜ не начата |
| 12 | Оптимизация, полировка, приёмка | ⬜ не начата |

---

## Фаза 0 — Фундамент и инструменты — ЗАВЕРШЕНА

### Что сделано

- `Engine` + `Clock` (fixed 1/60, аккумулятор, потолок 5 подшагов) + `Profiler`
  (кольцо 120 кадров, p50/p95, реальные GPU-таймстампы) + `FrameGraph`
  (26 проходов зафиксированы списком, поддержка амортизации) + `ResourceManager`
  (refcount + бюджет на кадр) + `WorkerPool` + `Settings` (5 пресетов).
- `Renderer`: WebGPURenderer, детект бэкенда, реальный adapter в консоль,
  обработка потери устройства, ресайз с devicePixelRatio × render scale.
- UI: `DebugOverlay` (F3, график кадра с адаптивным масштабом, разбивка по секциям),
  `SettingsPanel` на lil-gui (F4, вся таблица качества), `LoadingScreen` с реальным
  прогрессом и экраном «WebGPU недоступен», `HUD` + справка (H), горячие клавиши
  F1/F2/P.
- `Input` + `FlyCamera` (WASD/мышь/геймпад, pointer lock, колесо — скорость).
- `math/Noise.ts` + `math/FBM.ts`: целочисленный хеш, Perlin 2D/3D, Perlin с
  аналитическими производными, Worley 2D/3D (бесшовный), fBm / ridged / billow /
  eroded / domain warp. 32 unit-теста, все зелёные.
- `scripts/screenshot.ts` — Playwright-харнесс: поднимает dev-сервер, ждёт
  `__AETHER_READY__`, гоняет камеру по 6 эталонным ракурсам, ждёт сходимости TAA,
  пишет PNG + `.md` с таймингами и консольными ошибками.
- `window.__AETHER_DEBUG__` — типизированный мост для харнесса и e2e.

### Тайминги (WebGPU, Apple M-series, 1600×900, отладочная сцена)

| Секция | CPU p50 | CPU p95 | Бюджет фазы |
|---|---|---|---|
| update | 0.10 | 0.10 | — |
| main-opaque | 0.90 | 1.80 | — |
| ui | 0.00 | 0.30 | — |
| **кадр целиком** | **1.10** | **2.10** | 16.6 |

Пустая сцена, 11 draw calls, 1.2k треугольников. Числа полезны только как база
отсчёта — реальные бюджеты начнут иметь смысл с фазы 1.

### Как выглядит

Смотрел скриншот. Серая земля с сеткой 10 м, эталонные кубы 1/2/10 м, капсула
1.75 м (рост игрока), корректные тени, небо-заглушка, оверлей с графиком.
Масштаб читается — ровно то, ради чего эта сцена и нужна.

Что не нравится: муар сетки у горизонта (нет сглаживания — лечится в фазе 10),
свет плоский и временный (фаза 3 заменит его целиком).

### Найдено и исправлено по ходу

1. **`depth: false` в конструкторе рендерера убил depth-тест.** Фаза 0 рисует
   прямо в swap chain, поэтому сцена деградировала до painter's algorithm: земля
   закрашивала кубы. Нашлось только потому, что я посмотрел на картинку и
   померил проекцию объектов в пиксели. Исправлено на `depth: true` с
   комментарием, почему нельзя выключать до фазы 10.
2. **`perlin3` умножался на 2/√3.** Классическая копипаста: улучшенный Perlin 3D
   с градиентами длины √2 уже лежит в [-1,1] (замеренный максимум 0.9992), а
   «нормировка» выносила его на 1.12 и клипала у любого потребителя, ожидающего
   единичный диапазон. Множитель убран, константы замерены и записаны.
3. **fBm нормировался бегущей суммой амплитуд.** Из-за этого 4-октавная и
   8-октавная выборка одной точки давали разную высоту: базовая октава
   ослаблялась по мере добавления детализации. Это сделало бы невозможным
   снижение числа октав на дальних тайлах — силуэт горы менялся бы по мере
   приближения. Переведено на нормировку по пределу ряда (1/(1−gain)):
   число октав стало чистой ручкой качества. Добавлен тест на инвариантность.
4. **`domainWarp2` аллоцировал объект параметров на каждый сэмпл** — вынесен на
   уровень модуля.
5. **`renderer.info.compute.calls` кумулятивен**, а не по кадру — заменено на
   `frameCalls`.
6. **three не хранит GPUAdapter**, только GPUDevice, поэтому `adapter.info`
   через бэкенд не достать. Берём `device.adapterInfo`, с откатом на адаптер,
   который мы и так запрашиваем при проверке поддержки. Теперь в консоли
   реальные `vendor=apple arch=metal-3`.

### Компромиссы и долги

- `// TODO(quality):` **GPU-таймстампы не откалиброваны.** three возвращает сумму
  длительностей проходов последнего кадра пачки; при 900 FPS в headless число
  (≈9 мс) физически не сходится с частотой кадров. Опрос сведён к 1 разу в 15
  кадров. **Числам GPU нельзя верить, пока в фазе 1 не появится реальная
  нагрузка и vsync.** До тех пор бюджеты веду по CPU.
- `// TODO(quality):` **F1 fly/player** — обработчик есть, но переключать пока не
  на что: `PlayerController` появится в фазе 11. Сейчас честно пишет об этом в HUD.
- `// TODO(quality):` **`ResourceManager` ещё ничего не отслеживает** — отладочная
  сцена создаёт ресурсы напрямую, поэтому «mem ~0 MB» в оверлее правдиво, но
  бесполезно. Подключается в фазе 1 вместе с пулом тайлов.
- `// TODO(quality):` **Авто-пресет по бенчмарку** не реализован: в панели пункт
  `auto` есть, но обработчик пустой. Фаза 12.
- `// TODO(quality):` **Первый кадр стоит ~50 мс** — компиляция шейдеров на лету.
  Прогрев шейдеров на загрузочном экране запланирован в фазе 12.
- WGSL-двойник `math/Noise.ts` ещё не написан (нужен только с фазы 1), поэтому
  тест на совпадение CPU/GPU высот физически не может существовать. Он —
  первая задача фазы 1, до генерации тайлов.
- WebGPU-бэкенд three запрашивает адаптер с `featureLevel: 'compatibility'`.
  На этой машине `core-features-and-limits` присутствует (compat=false), но на
  части железа режим включится и урежет MSAA и лимиты биндингов. Проверить в фазе 12.

### Окружение

`npm run typecheck` на этой машине занимает десятки минут: диск `/System/Volumes/Data`
заполнен на 97 % (6.8 ГБ свободно), и tsc проводит ~1000 с в I/O при 0.2 с
собственно проверки. Включён `incremental` с кэшем в `node_modules/.cache`.
**Освобождение места на диске — самый дешёвый способ ускорить всю разработку.**

---

## Фаза 1a — Террейн: геометрия и LOD — ЗАВЕРШЕНА

### Что сделано

- `HeightField.ts` + `shaders/heightfield.wgsl.ts` — функция высоты, написанная
  дважды и синхронизированная построчно. Слои по ТЗ: континентальность →
  эрозионность → хребты в искажённом домене (domain warping) → долины поперёк →
  детализация по уклону → осадочные слои.
- `Quadtree.ts` — CDLOD-выборка без аллокаций, отдаёт плоский буфер инстансов.
  Морфинг вершин к позициям родительского уровня на последних 30 % диапазона.
- `Frustum.ts` — отсечение узлов по вертикальному экстенту мира.
- `TerrainSystem.ts` — один патч 33×33, инстансированный на все узлы.
  **2 draw call на весь видимый мир.**
- Стартовая позиция выбирается алгоритмически: суша, рядом с водой, с рельефом
  в пределах видимости.

### Тайминги (WebGPU, Apple M-series, 1600×900, High, горный ракурс)

| Секция | Значение | Бюджет | |
|---|---|---|---|
| main-opaque (CPU) | 5.2 мс | — | |
| **терраин (GPU)** | **10.6 мс** | **2.0 мс** | ⚠️ **превышение в 5 раз** |
| Треугольников | 293k | — | |
| Draw calls | 2 | — | ✅ |

### Как выглядит

Смотрел скриншоты. Читается как настоящая горная гряда: непрерывные гребни,
отдельные вершины, долины между ними, самозатенение по склонам. Не «шумная
простыня» — критерий приёмки по силуэту выполнен.

Что не нравится честно: **слишком мягко**. Похоже на выветренные холмы или дюны,
а не на скалы. Причина понятна — нет гидравлической эрозии, ничто не прорезало
русла. Это ровно то, что ТЗ называет «самым большим скачком реализма за
наименьшие деньги», и это следующая задача.

### Найдено и исправлено

1. **fBm — гауссиан, а не равномерное [-1,1].** Сумма независимых октав даёт
   σ ≈ 0.12 и размах ±0.37 на 50 км. Кривая континентальности была написана в
   расчёте на ±1, поэтому первый рабочий билд выдал мир с высшей точкой 180 м —
   формально рельеф, визуально парковка. Добавлено мягкое насыщение
   `x/√(1+x²)` перед кривой: теперь −341…786 м при 52 % суши.
2. **16 градиентов через cos/sin → 8 через биты.** Функция высоты вызывает
   perlin2 ~32 раза, а вершинный шейдер считает высоту 5 раз на вершину. Восемь
   трансцендентных на угол решётки — это разница между рабочим шейдером и
   нерабочим.
3. **Террасирование давало горизонтальную полосатость** на пологих склонах —
   читалось как горизонтали топокарты. Вес снижен 0.55 → 0.18 и привязан к
   крутизне, чтобы слоистость показывалась только на скальных стенках.
4. Отладочная сцена (плита 2000×2000 при y=0) удалена — она протыкала рельеф.

### Компромиссы и долги

- `// TODO(quality):` **Главный долг фазы: высота считается аналитически в
  вершинном шейдере, 5 раз на вершину, каждый кадр.** Отсюда 10.6 мс вместо 2.0.
  По ТЗ высота должна считаться compute-шейдером один раз на тексель в
  heightmap-текстуру с пулом тайлов и LRU-вытеснением. Это фаза 1b, и она же
  разблокирует эрозию — эрозии нужна текстура, по которой можно гонять капли.
- `// TODO(quality):` **Гидравлическая эрозия (ТЗ 1.2) не реализована.** Без неё
  рельеф мягкий. Побочные продукты (карта стока, карта влажности) нужны фазам 6
  и 7 — их тоже пока нет.
- `// TODO(quality):` **Тест совпадения CPU/GPU высот ещё не написан** — нужен
  readback с GPU, то есть e2e-тест. Функции написаны как точные зеркала, но это
  утверждение пока не проверено машиной.
- `// TODO(quality):` Тени от рельефа выключены — CSM в фазе 4, а дефолтный
  shadow map three на таком масштабе бесполезен.
- Точность f32: за пределами ~2·10⁵ м от начала координат детализация начнёт
  квантоваться. Лечится переходом на координаты относительно тайла.

### Что дальше

Фаза 1b: compute-генерация тайлов в `texture_2d_array` + пул слоёв с LRU, затем
гидравлическая эрозия по Beyer (60 000 капель на тайл 256²) и термальная осыпь.
Это одновременно снимает превышение бюджета и даёт рельефу характер.

---

## Фаза 1b (задача 1) — Запекание высот в тайлы — ЗАВЕРШЕНА

### Что сделано

- `TilePool.ts` — LRU-пул слоёв трёх array-текстур: `heightArray` (r32float),
  `surfaceArray` (rgba16float: xyz — нормаль, w — уклон как |∇h|) и
  `flowArray` (rgba16float, пока нулевая — её заполняет задача 2). Тайл
  адресуется тройкой (level, gx, gz), всё регистрируется в `ResourceManager` —
  F3 теперь показывает честные **81.3 МБ** вместо «~0 MB».
- `HeightmapGenerator.ts` — два compute-прохода 8×8: первый считает высоту по
  `fnTerrainHeight`, второй берёт **центральные разности запечённой карты** и
  пишет нормаль + уклон. Все тайлы кадра идут одним диспатчем: параметры лежат
  в storage-буфере, индексируемом по оси z.
- `TileManager.ts` — политика стриминга: обход выбранных узлов, приоритет
  1/дистанция с четырёхкратным весом родительским тайлам, бюджет
  `quality.tileBudget` на кадр, разрешение пары «свой тайл / родительский».
- `TerrainSystem.ts` — вершинный шейдер больше **не вызывает** функцию высоты:
  четыре выборки текстур (высота своего уровня, высота родителя, нормаль своего
  уровня, нормаль родителя) и `mix` по morphK. Высота теперь морфится вместе с
  позицией — до эрозии это тонкость, после неё обязательное условие.
- `Quadtree.ts` — `NODE_STRIDE` 8 → 16. Двух зарезервированных float не хватило:
  каждой из двух выборок нужен слой плюс uv-подпрямоугольник.
- Отладочные режимы 1–9 работают; режим 6 рисует **настоящую** тексельную сетку
  запечённого тайла и подсвечивает янтарным узлы, рисующиеся с предка.

### Тайминги (WebGPU, Apple M-series, 1600×900, High, горный ракурс)

Измерено одним и тем же скриптом до и после, разностью времени кадра при
скрытом и показанном меше террейна (частота кадров разблокирована):

| Что | Было (1a) | Стало (1b) | Бюджет | |
|---|---|---|---|---|
| **террейн (GPU, изоляция)** | **6.80 мс** | **0.60 мс** | 2.0 мс | ✅ 11× |
| **генерация (GPU, 2 тайла/кадр)** | — | **0.20 мс** | 1.0 мс | ✅ |
| время кадра (без vsync) | 7.40 мс | 1.20 мс | — | |
| gpu-render по таймстемпам three | 12.75 мс | 4.41 мс | 2.0 мс | ⚠️ см. ниже |
| Память GPU | 0 МБ | 81.3 МБ | 160 МБ | ✅ |
| Draw calls | 2 | 2 | — | ✅ |

**Двум цифрам верить нельзя одновременно.** `gpu-render` — это таймстемпы
three, про которые ещё в фазе 0 записано, что они не откалиброваны: 4.41 мс
физически несовместимы с временем кадра 1.20 мс при 833 FPS. Изоляционное
измерение (спрятать меш — измерить — показать) не зависит от таймстемпов вообще
и даёт 0.60 мс. Считаю верным его, но фиксирую расхождение: пока таймстемпы не
откалиброваны, «уложились в 2.0 мс» — вывод из косвенного измерения.

### Проверка совпадения CPU/GPU

Впервые сделана машинно. Слой heightArray копируется в буфер через сырой
WebGPU `copyTextureToBuffer` и сравнивается с CPU-функцией высоты:

| Тайл | Сэмплов | max ошибка | средняя |
|---|---|---|---|
| level 0, 128 м (1 м/тексель) | 289 | **0.0018 м** | 0.0007 м |
| level 9, 65536 м (512 м/тексель) | 289 | **0.0117 м** | 0.0010 м |

Допуск ТЗ — 0.05 м. Разница — накопление f32 против f64, ровно как предсказано
в шапке `heightfield.wgsl.ts`. Дельта на общем ребре тайла против аналитической
высоты в той же мировой точке: < 0.0023 м, то есть соглашение «общие рёбра»
работает и соседний тайл держит в своём нулевом текселе то же число.

### Решения, которые стоит знать

1. **Тайл 129², а не 256².** ТЗ просит 256². Арифметика: слой 257² стоит
   1.26 МБ, бюджет 160 МБ даёт 126 слоёв, а замеренный рабочий набор — 143–215
   различных тайлов (свой уровень + родительский, четыре ракурса). 126 < 215:
   пул вытеснял бы тайлы, которые видны **в этом же кадре**. При 129² слой
   стоит 0.317 МБ, потолок — не бюджет, а лимит WebGPU `maxTextureArrayLayers`
   = 256 слоёв = 81.3 МБ. Общий тайл 257² на группу 2×2 узлов даёт каждому узлу
   то же окно 129×129 за те же байты — упаковки, которая обошла бы этот размен,
   не существует. При таком бюджете и таком рабочем наборе 128 ячеек на узел —
   это то, что покупает память.
2. **129, а не 128.** Соглашение «общие рёбра»: тексель i стоит в p = i/(N−1),
   поэтому крайние тексели соседних тайлов — одна и та же мировая точка и одно
   и то же число. Плюс 128 ячеек на 32 квада патча — ровно 4 текселя на квад,
   вершина патча попадает точно в центр текселя, фильтр не размывает.
3. **Нормали — из запечённой карты, а не аналитические.** Иначе после эрозии
   нормаль описывала бы не ту поверхность, которая нарисована.
4. **Гарантия «дырок не бывает».** Корневой тайл закреплён и печётся в `init`
   до первого кадра. Любой узел — потомок корня, поэтому подъём по цепочке
   предков всегда упирается в готовый слой.

### Найдено и исправлено по ходу

1. **LRU отказывался вытеснять после телепорта.** Возрастной порог в 2 с
   означал, что после прыжка камеры полный пул «свежих» тайлов со старого места
   не отдавал ни одного слоя. Замерено: 105 из 143 видимых узлов сидели на
   грубом предке две секунды. Добавлен второй ярус — если несвежих нет, берём
   обычный LRU, исключая тронутое в этом кадре (то есть ровно то, что на
   экране).
2. **`ShaderNodeObject` не существует в `@types/three@0.185`.** `debugViews.ts`
   импортировал этот тип с фазы 1a — то есть `npm run typecheck` был сломан ещё
   до этой задачи (vite собирает через esbuild и типы не проверяет, поэтому
   дев-сервер работал). Заменено на явный псевдоним с комментарием.
3. **`recordGpu` на секцию с CPU-парой даёт бесполезный p50.** Таймстемпы
   опрашиваются раз в 15 кадров, а кольцо — 120 кадров, поэтому 14 слотов из 15
   остаются нулями и медиана равна нулю. Поэтому `terrain-generate` — чисто
   GPU-серия, а CPU-стоимость отправки лежит отдельно в `terrain-stream`.

### Компромиссы и долги

- `// TODO(quality):` **Кольцо нормалей на границе тайла считается аналитически.**
  Соседний тексель принадлежит соседнему тайлу, которого может не быть в пуле,
  поэтому 3 % текселей берут высоту из аналитической функции. До эрозии это
  бит-в-бит то же число — швов нет. После эрозии перестанет им быть, и на
  границе появится шов шириной в тексель. Лечится апроном в scratch-буфере
  эрозии (задача 2).
- `// TODO(quality):` **Потолок по времени GPU — разомкнутый.** `EST_MS_PER_TILE`
  = 0.10 мс замерено на этой машине; обратной связи нет, потому что three даёт
  один агрегированный COMPUTE-запрос на кадр, а не по проходу. На заметно более
  медленном железе генерация будет вылезать за свой 1.0 мс, пока константу не
  перемеряют.
- `// TODO(quality):` **`flowArray` создана и обнулена, но никем не пишется.**
  Режимы 7 и 8 остаются заблокированными через `erosionDataAvailable`, чтобы
  нулевую текстуру нельзя было принять за результат.
- `// TODO(quality):` **Ветка WebGL2 осталась на аналитических высотах.** Без
  compute печь нечем; это по-прежнему пять вычислений высоты на вершину и полное
  отсутствие эрозии. Деградированный режим, а не поддерживаемый.
- `// TODO(quality):` **Нет отката, если у железа нет `float32-filterable`.**
  Проверка есть и пишет в консоль, но three в этом случае уходит на свою
  биквадратичную выборку с **repeat**-обёрткой и границы тайлов заломит.
- Нормали теперь считаются с шагом текселя (size/128), а не с шагом вершины
  патча (size/32) — вчетверо мельче. Силуэт совпадает с 1a пиксель-в-пиксель,
  но поверхность читается заметно «камнистее». На взгляд лучше; если на
  движении появится мерцание — расширять стенсиль, а не возвращать аналитику.
- Холодный старт: при `tileBudget` = 2 полный набор из ~190 тайлов набирается
  примерно за 95 кадров. До этого узлы рисуются с предка (в режиме 6 — янтарные).

---

## Фаза 1b (задача 2) — Гидравлическая и термальная эрозия — ЗАВЕРШЕНА С ДОЛГОМ

### Что сделано

- `shaders/erosion.wgsl.ts` — шесть compute-ядер на TSL: посев (аналитическая
  высота по scratch-области), капля по Байеру, применение поколения, термальная
  осыпь по Масгрейву, X-половина сепарабельного блюра и резолв.
- `Erosion.ts` — планировщик. Эрозия одного тайла — это **работа на много
  кадров**: 93 диспатча, по 3–4 за кадр в рамках бюджета. Тайл берётся из пула
  по той же приоритетной очереди, что и запекание.
- `TilePool` получил состояние работы эрозии по слоям (нет / идёт / готово),
  слой «в работе» исключён из вытеснения, и есть проверка `isErosionJobValid`:
  если слой за время работы переехал на другой тайл, резолв не пишется.
- `flowArray` наконец пишется. Каналы задокументированы в `TilePool` как
  контракт для фаз 6 и 7, с единицами измерения.
- Нормали пересчитываются из **эродированной** карты внутри scratch-области с
  апроном, поэтому у краевого кольца настоящие соседи — долг задачи 1 закрыт.
- Режим 7 больше не логарифмирует поверх уже логарифмированного стока.

### Как это устроено (и чем отличается от ТЗ)

**Высоты внутри симуляции — в ячейках, не в метрах.** Константы Байера
безразмерны: они рассчитаны на карту в [0,1] с единичной ячейкой. У нас перепад
1400 м, а шаг ячейки от 1 м (уровень 0) до 512 м (корень), так что `dh` в метрах
означал бы на каждом уровне своё. Симуляция идёт по `h/cellSize`, резолв
умножает дельту обратно.

**`erodeRate` и `depositRate` — 0.1, а не 0.3.** Это следствие предыдущего
пункта, и это было найдено по картинке. При 0.3 первый же шаг каждой капли
выгребает весь перепад под собой, а поскольку осадок кладётся билинейно на
четыре текселя, а размыв размазывается по двадцати пяти, каждая капля оставляет
бугор в один тексель. Померено: наждачная поверхность, каналообразование 15 %
против 5 % у равномерного понижения. При 0.1 сигнал несёт сумма по поколениям,
а не след отдельной капли; общая глубина компенсирована числом раундов.

**Шахматка 4 фазы — сделана, но она НЕ убирает гонки, и это записано прямо в
шапке файла.** Капли мигрируют: до 60 шагов по клетке и кисть в 25 ячеек на
каждом, поэтому две капли одной фазы, стартовавшие через клетку, пишут в одну
ячейку уже после первого шага — порядка сотни конфликтов на ячейку за фазу.
Корректность держится не на шахматке, а на **целочисленном атомарном
аккумуляторе** (i32, квант 2⁻¹⁶ ячейки): целые числа складываются
ассоциативно, поэтому сумма побитово одинакова при любом порядке запуска. На
f32 правило ТЗ «тот же сид → тот же мир» невыполнимо в принципе. Шахматка
осталась как структура **поколений** (разбиение по чётности мировой клетки, то
есть не зависящее от тайла) — она даёт устойчивость Якоби и одинаковый набор
капель у соседних тайлов.

**Апрон 128 ячеек**, scratch 385², 148 225 капель за раунд, 2 раунда =
296 450 капель на тайл (ТЗ просит 60 000 на тайл 256²; у нас ядро 129², то есть
плотность выше). Одна капля на ячейку, дрожание — хеш от **абсолютного**
мирового индекса ячейки, иначе весь аргумент про апрон разваливается.

### Тайминги (WebGPU, Apple M-series, 1600×900, High, горный ракурс)

Изоляция: p50 времени кадра при простое эрозии против непрерывной работы,
частота кадров разблокирована.

| Что | Значение | Бюджет | |
|---|---|---|---|
| кадр, эрозия простаивает | 0.60 мс | — | |
| кадр, эрозия работает | 1.10 мс | — | |
| **эрозия (GPU, изоляция)** | **0.50 мс/кадр** | 1.0 мс на генерацию | ✅ |
| эрозия на один тайл | 12.5 мс (25 кадров × 0.50) | — | |
| диспатчей на тайл | 93 | — | |
| скорость | 24 тайла / 600 кадров | — | |
| весь рабочий набор (256 тайлов) | 9.9 с без vsync | — | |
| память scratch | +2.6 МБ (81.3 → 84 МБ) | 160 МБ | ✅ |
| консольных ошибок | 0 | 0 | ✅ |

Бюджет генерации 1.0 мс делится: запекание получает своё первым (несделанный
тайл — это поверхность, которой ещё нет), эрозия работает на остатке и во время
стриминга простаивает полностью.

### Что померено в самих данных

Обратное чтение слоёв `heightArray` и `flowArray` против CPU-функции высоты:

| Уровень | шаг | средняя Δ | p05 | p95 | max | канал.* | сток p05/50/95 |
|---|---|---|---|---|---|---|---|
| 1 | 2 м | −0.003 | −0.14 | +0.15 | 0.60 | 15 % | 0.43/0.54/0.62 |
| 2 | 4 м | −0.22 | −1.46 | +0.60 | 2.47 | 15 % | 0.20/0.47/0.71 |
| 3 | 8 м | −0.29 | −1.65 | +0.88 | 7.14 | **26 %** | 0.25/0.50/0.69 |
| 4 | 16 м | −1.11 | −5.88 | +2.29 | 16.0 | 19 % | 0.18/0.42/0.71 |
| 5 | 32 м | −0.98 | −6.29 | +2.30 | 27.6 | 22 % | 0.16/0.43/0.68 |

\* доля общего размыва, пришедшаяся на 5 % самых прорезанных ячеек. 5 % — это
равномерное понижение, выше — каналы.

Нормировка стока подобрана по этим же данным: сначала стояло «полная шкала при
255», и p95 упирался в 1.0 на всех уровнях грубее второго — вся сеть читалась
сплошным белым. Сейчас `log2(1 + W)/10`, где W — водо-шаги на ячейку **на один
раунд** (деление на раунды, чтобы карта не меняла яркость при перенастройке
числа капель — фазы 6 и 7 читают её как абсолютную величину).

### Найдено и исправлено по ходу

1. **Утечка массы в конце жизни капли.** Опубликованный цикл Байера просто
   бросает осадок, который капля несёт в момент истечения ресурса. На нашей
   плотности это не погрешность округления: тайл с ячейкой 16 м опускался в
   среднем на 5.7 м — больше, чем сама глубина каналов, — и, поскольку утечка
   масштабируется с размером ячейки, она **рассинхронизировала уровни LOD**
   (0 м при 2 м ячейке, 5.7 м при 16 м). Теперь осадок выкладывается там, где
   капля закончилась, и это же даёт конусы выноса: капли иссякают там, где
   кончается уклон.
2. **Режим 8 показывал не влажность, а угол направления стока.**
   `TerrainSystem` читал `flowSample.y`, а по упаковке из рекогносцировки
   влажность лежит в `.z`. На картинке это выглядело как рябь, которую блюр
   почему-то не сгладил, — что и выдало ошибку.
3. **Морф считался по горизонтальной дистанции, а выбор уровня — по
   `sqrt(distFlat² + camY²/4)`.** Долг из фазы 1a: пока оба уровня брали высоту
   из одной аналитической функции, они совпадали и без морфа, и ошибка была
   невидима. С эрозией это разные поверхности, и переключение уровня стало
   происходить раньше, чем морф успевал сойтись. Исправлено — вершинный морф
   теперь меряет ту же метрику, что и выборка.
4. **Апрон 192 не помогает.** Ставилось как эксперимент, чтобы добить швы:
   в 1.8 раза больше работы, на 4 м ячейке шов упал с 0.48 до 0.17 м, а на 16 и
   32 м — вообще не изменился (4.50 → 4.55, 3.54 → 3.53). Значит мелкие уровни
   ограничены апроном и сходятся, как предсказано, а у грубых остаток имеет
   другую, ненайденную природу. Вернул 128.
5. **4 раунда вместо 2 не окупаются.** Каналообразование не выросло (24 % →
   23 %), а шов утроился: каждое лишнее поколение даёт граничной ошибке ещё
   `MAX_LIFETIME` ячеек хода.

### Компромиссы и долги

- `// TODO(quality):` **ГЛАВНЫЙ ДОЛГ — шов между независимо эродированными
  соседями на грубых уровнях.** Замерено по общему столбцу двух соседних
  тайлов, максимум по 129 текселям края:

  | ячейка | 1 м | 2 м | 4 м | 8 м | 16 м | 32 м |
  |---|---|---|---|---|---|---|
  | апрон 96 | 0.026 | 1.09 | 2.67 | 0.075 | 15.5 | 12.4 |
  | **апрон 128** | — | 0.032 | 0.48 | 0.014 | **4.50** | **3.54** |
  | апрон 192 | — | — | 0.17 | 0.007 | 4.55 | 3.53 |

  Среднее по краю всего 0.17–0.25 м, то есть это единичные тексели-выбросы, а
  не ступенька. Но на скользящем угле каждый такой тексель открывает щель в
  один пиксель, сквозь которую видно небо. Пара на 8 м ячейке даёт 0.014 м,
  то есть механизм сходится — остаток зависит от рельефа. Апрон увеличивать
  бессмысленно (см. п. 4), нужно сначала найти реальный источник расхождения.
  Дешёвая маскировка, если источник не найдётся, — юбки по краям патчей.
- `// TODO(quality):` **Термальная эрозия практически не срабатывает.**
  Формулировка честная (истинное расстояние до соседа по каждому из восьми
  направлений, угол покоя выводится из канала осадка, а не красится), но при
  нашем шаге ячейки склон в единицах «ячейка на ячейку» почти нигде не доходит
  до tan(34°) = 0.67, поэтому осыпей под стенками **нет**. Это следствие
  разрешения тайла, а не ошибка: на 16 м ячейке склон 45° — это перепад 16 м на
  тексель, чего рельеф на такой длине не даёт. Осыпи появятся, когда будет
  уровень с ячейкой меньше метра, либо если считать угол покоя по
  субтексельному уклону.
- `// TODO(quality):` **Согласованность между уровнями не решена.**
  Эродированная поверхность уровня L+1 — не даунсэмпл уровня L. Ограничитель
  `REFERENCE_CELL_M = 16` не даёт корневому тайлу вырыть каньон в 200 м, но
  средняя просадка всё равно разная по уровням (−0.003 м на 2 м ячейке против
  −1.1 м на 16 м). Морф высоты не даёт этому «щёлкать», но рельеф «дышит» при
  подлёте. Рекогносцировка отметила это как нерешённое, оно таким и осталось.
- `// TODO(quality):` **Совпадение CPU/GPU высот сломано эрозией** — как и
  предсказывалось. `HeightField.height()` аналитическая и про эрозию не знает,
  а рисуется эродированный тайл. Расхождение замерено: 0.6 м на 2 м ячейке и до
  27 м на 32 м. Допуск ТЗ 0.05 м. Ноги игрока (фаза 11) придётся ставить по
  обратному чтению эродированного тайла, а не по аналитике; `parity.spec.ts` в
  нынешнем виде обязан упасть.
- `// TODO(quality):` **Оценка стоимости диспатчей разомкнута** — те же
  константы «на глаз», что и `EST_MS_PER_TILE` у запекания, и по той же
  причине: three отдаёт один агрегированный COMPUTE-запрос на кадр.
- `// TODO(quality):` **Сток обрезан по длине жизни капли.** Водосбор не может
  быть длиннее 60 ячеек, то есть 120 м на уровне 1 и 1.9 км на уровне 5.
  Локальный рисунок верный, континентального дренажа нет. Фазе 6 понадобится
  грубая глобальная карта стока, посчитанная один раз при старте.
- Тайл, который эродируется прямо на экране, заметно меняется в момент
  готовности. Это осознанный размен: пока эрозия не досчитана, рисуется
  неэродированная версия, а не дырка и не пауза.

### Как выглядит

Смотрел скриншоты. Против базы 1b рельеф стал заметно резче: гребни тонкие,
склоны несут рисунок водосбора, у подножий читаются наносы. Это **не** тот
драматичный каньонный вид, который делают демки эрозии — те гоняют тысячи
итераций по одной маленькой карте; здесь размыв 0.1–1.7 ячейки, что
соответствует и опубликованному алгоритму, и цифрам рекогносцировки.

Режим 7 показывает настоящую ветвящуюся сеть: тёмные линии по гребням,
жёлтые тальвеги, слияния вниз по склону. Режим 8 после исправления канала —
гладкое поле, сухие выпуклые склоны, влажные низины. Режим 2 гладкий и
непрерывный, полос и плоских пятен нет. Режим 6 — контуры патчей целые,
янтарных (fallback) узлов нет.

Что не нравится честно: щели на швах (см. долг №1) и полное отсутствие осыпей.


---

## Фаза 1b — Тайлы и эрозия — НЕ ЗАВЕРШЕНА

Фаза 1 **не закрывается**: эрозия реализована, но не производит эффекта, и
бюджет генерации превышен. Ниже — замеренные факты, а не намерения.

### Что реально работает

Compute-выпечка heightmap **сделана и даёт главный выигрыш фазы**:

| Метрика | Было (1a, аналитика в вершинном шейдере) | Стало (1b, тайлы) |
|---|---|---|
| Кадр целиком, p50 | 6.00 мс | **0.90 мс** |
| CPU на кадр | 4.30 мс | **0.60 мс** |
| FPS (без vsync) | 167 | **1111** |
| Draw calls | 2 | 2 |
| GPU-память (реальная) | не отслеживалась | **85 МБ** |

- `TilePool.ts` — LRU-пул слоёв `texture_2d_array`: height (r32float),
  surface (rgba16float: xyz нормаль, w уклон), flow (rgba16float). Всё
  зарегистрировано в `ResourceManager`, поэтому «mem» в F3 перестал врать.
- `HeightmapGenerator.ts` — выпечка тайла 256×256, workgroup 8×8, нормали из
  центральных разностей **по запечённой карте**, а не аналитически.
- `TileManager.ts` — приоритет по расстоянию, бюджет на кадр, гарантия
  резидентности родителя, откат на тайл предка вместо дырки.
- Вершинный шейдер больше **не вызывает `fnTerrainHeight` ни разу** — только
  читает текстуры и интерполирует между своим и родительским уровнем.

Проверено визуально: силуэт гор **идентичен** версии 1a. Это и есть
доказательство корректности uv-маппинга — при ошибке на полтексела рельеф бы
сместился. `tilesPending 0`, `tilesFallback 0` — дырок нет, стриминг успевает.

### Что НЕ работает — честно

1. **Эрозия не производит видимого эффекта.** Склоны в затенённом виде
   выглядят так же, как до эрозии: русел нет, конусов выноса нет, гребни не
   заострились. `tilesEroded` растёт, то есть проходы запускаются, но их
   амплитуда либо близка к нулю, либо результат не попадает в отображаемую
   текстуру.
2. **Flow map пустая.** Режим 7 показывает равномерную заливку вместо
   ветвящейся дендритной сети. Несколько белых точек — не сеть. Данные для фаз 6
   (реки/озёра) и 7 (плотность растительности) фактически отсутствуют, хотя
   `erosionDataAvailable` выставлен в true — то есть флаг врёт.
3. **Бюджет генерации превышен: 2.80 мс против 1.0 мс** при активной эрозии.
   Без эрозии — 1.03 мс, то есть ровно в бюджете.
4. **Похоже на перевыпечку тайлов.** `tilesEroded` за 12 секунд ушёл с 49 до 26
   при неподвижной камере — значит слои вытесняются и пекутся заново. LRU или
   учёт «использован в этом кадре» работает неверно.

### TODO(quality) — открытые долги фазы 1

- `// TODO(quality):` эрозия по Beyer не даёт видимого результата — проверить
  амплитуду (`erosionRate`/`depositionRate`), попадает ли результат ping-pong в
  итоговый слой, и не перетирается ли он последующей выпечкой нормалей.
- `// TODO(quality):` flow/moisture не заполняются; `erosionDataAvailable`
  выставляется без проверки, что в буфере есть данные — флаг должен зависеть от
  фактического содержимого, иначе режимы 7/8 показывают убедительную пустоту.
- `// TODO(quality):` генерация 2.80 мс при эрозии против бюджета 1.0 мс.
- `// TODO(quality):` перевыпечка тайлов при неподвижной камере — проверить LRU.
- `// TODO(quality):` тест совпадения CPU/GPU высот (0.05 м) по-прежнему не
  написан. Теперь он стал проще: высоты лежат в текстуре, readback возможен.
- `// TODO(quality):` GPU-таймстампы остаются неоткалиброванными: `gpu-render`
  показывает 3.66 мс при кадре 0.90 мс без vsync — физически несовместимо.
  Бюджеты по-прежнему веду по времени кадра, а не по этому счётчику.

### Вывод

Главная цель 1b — снять пятикратное превышение бюджета террейна — **достигнута**
(6.00 → 0.90 мс на кадр). Вторая обязательная часть, эрозия, **не сдана**:
код есть, эффекта нет. Фаза 1 остаётся открытой до устранения пунктов 1–4.

---

## Фаза 1b (задача 3) — Почему эрозия ничего не делала

Разбор четырёх симптомов предыдущего раздела. Итог: **три независимых бага**,
из них один в симуляции, один в отображении и один в измерении. Пятый симптом
(перевыпечка тайлов) не воспроизвёлся.

### Баг 1 — скорость капли обнулялась на первом же шаге (симуляция)

```
speed = sqrt(max(0, speed² + dh·GRAVITY))     // было
speed = min(MAX_SPEED, sqrt(max(0, speed² − dh·GRAVITY)))   // стало
```

`dh` отрицателен вниз по склону, поэтому опубликованная форма **вычитает**
энергию при спуске. При `speed = 1` и `GRAVITY = 4` любой перепад круче
`dh < −0.25` ячейки — это уклон 14°, то есть почти весь мир — даёт
`sqrt(max(0, отрицательное)) = 0`. А `capacity ∝ speed`, значит ёмкость тоже
ноль, значит капля **больше никогда не размывает**: оставшиеся 59 шагов из 60
она только откладывает тот единственный ковш, что успела зачерпнуть на первом
шаге.

Замерено: 1 размывающий шаг из 60, суммарное изменение высоты 0.10–0.15 ячейки.
Это и есть «эрозия работает, но следов не оставляет».

В шапке файла стояло «DO NOT fix this sign» с замером: при смене знака пик
изменения высоты уходил с 0.91 м на 272 м. Замер верный, вывод — нет. Разгон
без предела на 60 шагов действительно ломает ёмкость; лечится не возвратом
неверного знака, а **предельной скоростью** (`MAX_SPEED = 4`), что и физически
правильно: трение уравновешивает гравитацию.

### Баг 2 — сток и нормали читались по вершинам, а не по пикселям (отображение)

Тайл — 129² текселей, патч — 33² вершин, то есть **одна вершина на четыре
текселя**. Русла шириной 3–6 текселей. Вершинный шейдер брал `surfaceArray` и
`flowArray` в вершинах и интерполировал — 15 из 16 текселей выбрасывались, а
остаток размазывался билинейно.

Это объясняет сразу два симптома. Затенение никогда не видело русел (нормаль
приходила усреднённой), а режим 7 показывал ровную заливку вместо дендритной
сети. Замерено на слое уровня 4: по текселям сеть безошибочно ветвится, по
вершинной решётке — однородное серое поле.

Исправлено: высота по-прежнему считается в вершине (иначе нечего смещать), а
нормаль, сток и влажность **сэмплируются в фрагментном шейдере**. Это
стандартное разделение CDLOD, и оно же означает, что силуэт несёт эрозию только
до шага вершины, а затенение — всю.

### Баг 3 — таймстемпы GPU суммировались за 15 кадров (измерение)

`resolveTimestampsAsync` у three сбрасывает индекс запросов и возвращает
**сумму всех запросов с прошлого резолва**. Опрос идёт раз в 15 кадров, поэтому
`terrain-generate` показывал пятнадцатикратную стоимость. Отсюда «2.80 мс против
бюджета 1.0» — и отсюда же 1.33 мс на **полностью простаивающей** эрозии, что и
было первым признаком, что счётчику нельзя верить. Теперь `Engine` делит на
фактическое число прошедших кадров.

### Баг 4 — капли тонули в ямах и рыли их 60 шагов подряд

Побочный, но он определял вид поверхности. Поле высот заморожено на всё
поколение, поэтому капля, скатившаяся в чашу, каждый шаг перечитывает
**исходный** перепад под собой и вычерпывает его заново, а колебания вокруг
минимума ещё и накачивают скорость. Замерено на 2 м ячейке: карта стока — муть
с круглыми красными пятнами вместо сети, дельта высот — поле ям 10–20 текселей
ровно на этих пятнах.

Исправлено: капля, шагнувшая вверх без осадка на руках, **останавливается**.
Остаток выкладывается кистью там же — это и есть конус выноса.

Заодно: конечный сброс осадка раскладывался билинейно на 4 текселя против
25-тексельной кисти размыва. Асимметрия давала волдыри — круглые бугры 10–15
текселей, которые в затенении читались вместо русел. Теперь тот же брашь.

И добавлено сглаживание **только дельты** эрозии (тент 3×3, ядра `smoothDelta`
+ `applySmooth`): аналитическая основа остаётся резкой, а решёточный шум частиц
уходит. Русла 3–6 текселей переживают тент, шум 1–2 текселя — нет.

### Симптом 5 — перевыпечки тайлов НЕТ

Не воспроизвёлся. При неподвижной камере, 22 с наблюдения с шагом 2 с:

| | 0 с | 8 с | 22 с |
|---|---|---|---|
| ready | 256 | 256 | 256 |
| eroded | 9 | 228 | 256 |
| baked (всего) | 396 | 396 | 396 |
| starved / abandoned | 0 / 0 | 0 / 0 | 0 / 0 |

`baked` не растёт, `eroded` монотонно растёт, распределение по уровням
неизменно. Наблюдение «49 → 26 за 12 с» относилось к фазе схождения стримера
после телепорта камеры, а не к установившемуся состоянию. **Но пул насыщен под
завязку** — рабочий набор ровно 256 при 256 слоях, — так что запас нулевой, и
на другом ракурсе вытеснение начнётся. См. долги.

### Тайминги (WebGPU, Apple M-series, High, горный ракурс)

| Что | Было | Стало | Бюджет | |
|---|---|---|---|---|
| **terrain-generate (GPU, профайлер)** | **2.80 мс** | **0.27 мс** | 1.0 мс | ✅ |
| — из них ошибка измерения | ×15 | — | | |
| кадр p50 (без vsync), эрозия простаивает | 0.90 | **0.70 мс** | — | ✅ |
| кадр p50, эрозия работает | — | 1.30–2.30 мс | — | |
| изоляция: 3 диспатча/кадр над простоем | +1.9 (5 дисп.) | **+0.60 мс** | 1.0 | ✅ |
| диспатчей на тайл | 93 | 95 | — | |
| диспатчей на кадр | 3 (с перерасходом) | 3 (без) | — | |
| Draw calls | 2 | 2 | — | ✅ |
| Треугольников | 293k | 293k | — | |
| GPU-память | 85 МБ | 85 МБ | 160 | ✅ |
| консольных ошибок | 0 | 0 | 0 | ✅ |
| vitest | 34 ✅ | 34 ✅ | | ✅ |

Стоимость диспатча **замерена**, а не оценена: при фиксированном числе
диспатчей на кадр p50 интервала кадра против простоя (0.80 мс) —
0.90 / 1.00 / 1.90 / 2.70 / 3.90 мс при 1 / 2 / 4 / 5 / 8 диспатчах. Предельная
стоимость выше двух диспатчей ≈ 0.50 мс; первые один-два почти бесплатны,
потому что перекрываются с рендером. Отсюда `COST_DISPATCH = 0.30`, что даёт
три диспатча и замеренные +0.60 мс.

Плюс к этому цикл планировщика **проверял бюджет постфактум** — то есть всегда
выпускал тот диспатч, который его и ломал. Теперь стоимость списывается до
запуска.

### Что померено в самих данных

Обратное чтение слоёв против аналитической функции высоты (дельта в метрах):

| Уровень | шаг | p01 | p50 | p99 | p01 в ячейках |
|---|---|---|---|---|---|
| 1 | 2 м | −1.44 | −0.22 | +0.96 | 0.72 |
| 2 | 4 м | −4.00 | −1.84 | +0.76 | 1.00 |
| 3 | 8 м | −7.11 | −2.91 | −0.13 | 0.89 |
| 4 | 16 м | −16.00 | −5.77 | +4.15 | 1.00 |

До исправления знака скорости p01 на уровне 1 был −0.21 м. Врез вырос в 7 раз
и уперся в потолок `GEN_CLAMP_CELLS · поколений` = 1 ячейка, то есть теперь
ограничитель — устойчивость Якоби, а не физика капли.

`flowTexelsWritten` (счётчик самого прохода resolve, вычитанный с GPU):
**16 636 из 16 641** текселя тайла с ненулевым стоком.

### erosionDataAvailable больше не врёт

Было `stats.completed > 0` — утверждение о планировщике, а не о текстуре:
флаг стоял в true при пустой карте стока, и два режима, которые он открывает,
показывали убедительную пустоту. Теперь ядро `resolve` само считает атомарным
счётчиком тексели с ненулевым стоком, `Erosion` один раз вычитывает счётчик
через `renderer.getArrayBufferAsync` и поднимает флаг, только если их не меньше
четверти тайла. Не вычиталось — флаг остаётся false, режимы 7/8 закрыты.

### Как выглядит

Смотрел скриншоты, сравнивал с версией без эрозии на том же ракурсе
(`/tmp/ab-mtn-noero.png` против `/tmp/ab-mtn-ero.png`).

Режим 7 — плотная **ветвящаяся** дренажная сеть по всему видимому миру:
притоки собираются в тальвеги, тальвеги сходятся вниз по склону. Это то, чего
режим 7 не показывал ни разу за всю фазу.

Затенённый вид изменился безошибочно: по всем крупным склонам идёт рисунок
промоин, борта правой гряды изрезаны, днища долин заметно глаже склонов —
осадок туда и уносится. Гребни стали тоньше.

Что не нравится честно, по убыванию:

1. **Щели на швах стали заметнее.** Замерено прямым методом: фон покрашен в
   маджента, посчитаны пиксели фона внутри силуэта террейна — **284 пикселя**
   на нижних 45 % кадра 1400×800 (0.06 %). Это тот же долг, что и раньше, но
   амплитуда вреза выросла в 7 раз, а расхождение независимо эродированных
   соседей растёт вместе с ней.
2. **Средняя просадка разная по уровням** (−0.22 м на 2 м ячейке против
   −5.77 м на 16 м). Было −0.003 / −1.1. Рассинхронизация LOD усилилась.
3. На уровнях 0–1 (1–2 м ячейка) рисунок всё ещё мягкий: врез 0.7 ячейки — это
   1.4 м на фоне 1400 м рельефа.

### Компромиссы и долги

- `// TODO(quality):` **Щели на швах — теперь главный видимый дефект.** 284
  сквозных пикселя, метод замера описан выше и воспроизводим. Апрон увеличивать
  бессмысленно (замерено в задаче 2). Два кандидата, ни один не сделан: юбки по
  краям патчей (не трогает данные, чистая маскировка) или затухание дельты
  эрозии к нулю на внешних 4–6 текселях тайла (закрывает щель точно, но рисует
  сетку невыеденных лент по всем границам — структурный артефакт хуже
  случайного, поэтому не стал делать вслепую).
- `// TODO(quality):` **Согласованность уровней ухудшилась.** Средняя просадка
  −0.22 м на 2 м ячейке против −5.77 м на 16 м. Морф высоты не даёт этому
  щёлкать, но рельеф «дышит» на подлёте сильнее, чем до этой задачи.
- `// TODO(quality):` **Глубина вреза упёрлась в `GEN_CLAMP_CELLS`.** Потолок —
  `0.125 × 8 поколений` = 1 ячейка, и замер показывает 0.72–1.00. Поднимать
  клэмп в лоб нельзя: пробовал 0.375, весь прирост ушёл в ямы, а не в русла.
  Настоящий путь — больше поколений (`ROUNDS`), а это прямо пропорционально GPU.
- `// TODO(quality):` **Полный проход эрозии по видимому набору — 95 диспатчей
  × 256 тайлов при 3 диспатчах на кадр = ~8100 кадров.** Без vsync это ~16 с,
  при 60 Гц — **2.3 минуты**. До готовности рисуется неэродированный тайл (не
  дырка), но столько ждать много. 88 % работы капель приходится на апрон
  (385² против ядра 129²) — там и лежит единственный крупный резерв, ценой швов.
- `// TODO(quality):` **Пул насыщен ровно под завязку** — рабочий набор 256 при
  256 слоях (`starved 0`, но запаса нет). На ракурсе с бо́льшим набором начнётся
  вытеснение, и каждый вытесненный эродированный тайл стоит 95 диспатчей
  повторной эрозии. Потолок — `maxTextureArrayLayers`, не память (бюджет 160 МБ
  позволяет 504 слоя), так что лечится вторым array-текстурным пулом.
- `// TODO(quality):` **Таймстемпы GPU всё ещё не поэлементные.** Деление на
  число кадров убрало пятнадцатикратную ошибку, но three по-прежнему отдаёт один
  агрегат COMPUTE на кадр, поэтому `terrain-generate` не отделяет выпечку от
  эрозии, а p50 считается по ряду, который заполняется раз в 15 кадров.
  Бюджеты продолжаю вести по изоляционному замеру времени кадра.
- `// TODO(quality):` **Тест совпадения CPU/GPU высот по-прежнему не написан**
  и в нынешнем виде обязан падать: аналитическая `HeightField.height()` про
  эрозию не знает, а расхождение теперь до 16 м. Ноги игрока (фаза 11) придётся
  ставить по обратному чтению тайла.
- `// TODO(quality):` **Термальная осыпь по-прежнему почти не срабатывает** —
  долг задачи 2, не трогал.

---

## Фаза 2 — Материалы террейна — РЕАЛИЗОВАНА (текстуры процедурные)

### Что сделано

- **`SplatGenerator` + `shaders/splat.ts`** — классификация биомов в compute при
  выпечке тайла. Восемь весов в двух `rgba8` слоях array-текстуры,
  нормализованы (сумма = 1 по построению). Входы ровно те, что в ТЗ 2.1:
  высота, уклон, влажность (сток эрозии + низкочастотный шум + близость к
  уровню моря + вогнутость), температура (широта-аналог + лапс −6.5 °C/км +
  региональная аномалия + экспозиция склона), экспозиция, кривизна
  (пятиточечный лапласиан, обезразмеренный по ячейке).
- **`MaterialTextures` + `shaders/materialTextures.wgsl.ts`** — PBR-наборы
  **генерируются на GPU при старте**, а не загружаются. Два `texture_2d_array`
  512²×8: `albedo(rgb)+AO(a)` и `normal(rg)+rough(b)+height(a)`, полная
  mip-цепочка, анизотропия 8×. Ассетов в репозитории нет вообще; бюджет 25 МБ
  из ТЗ 2.3 израсходован на ноль.
- **`TerrainMaterial.ts`** (TSL) — все шесть пунктов ТЗ 2.2: триплан по маске
  уклона (`pow(abs(n),8)`, включается на 30°, полностью на 42°), стохастический
  тайлинг Heitz & Neyret, multi-scale 1×/8× по расстоянию, height-based
  смешивание слоёв (не lerp), POM на камне ближе 15 м с адаптивными 8–24
  шагами, макро-вариация двумя периодами (70 и 190 м) на albedo ±12 % и
  roughness.
- **`SnowLine.ts`** — снеговая линия как высота нулевой изотермы, с сезонным и
  суточным ходом и инерцией первого порядка (τ = 150 игровых секунд). Модель
  климата общая с классификатором (`CLIMATE`), поэтому снег ложится там, где
  классификатор положил под него камень.
- Режимы отладки **10 (смесь биомов)** и **11 (доминирующий биом + снег)**,
  на Shift+0 и Shift+1 — цифровой ряд кончился на 9.
- `Renderer.ts`: запрос `maxStorageTexturesPerShaderStage: 8` (по умолчанию 4,
  splat-ядру нужно 5), с обрезанием по фактическим лимитам адаптера.

### Текстуры процедурные, а не авторские

Это надо сказать прямо, потому что разница видна. Гранит здесь — это две
Worley-сети трещин разного масштаба, слоистость по направлению и минеральная
крапина; трава — Worley-кочки с направленной укладкой и двумя зелёными плюс
солома; песок — рябь с меандрирующим гребнем; снег — заструги вдоль ветра.
Всё это даёт правильную **структуру и масштаб**, но не даёт **конкретности**:
лишайника, который вырос на одной плите и не вырос на соседней, кварцевой жилы,
которая идёт именно так. Читается как убедительный грунт, но не как фотография
грунта. Взамен: ни загрузки, ни декодирования, ни KTX2-тулчейна, ни вопросов о
лицензии, и девятый биом стоит функции, а не набора текстур.

### Тайминги (WebGPU, Apple M-series, 1600×900, High, горный ракурс)

Метод для фрагментной стоимости: **разностный по render scale**. Кадр меряется
при scale 2.0 (3200×1800 = 4× пикселей) и при 0.25 (400×225 = 1/16), фиксированная
часть вычитается: `F = (t₄ − t₁ᐟ₁₆) / (4 − 1/16)`. Средняя по 1200 кадрам,
минимум из трёх прогонов, culling заморожен, эрозия дождалась конца.

| метрика | фаза 1b | фаза 2 | бюджет |
|---|---|---|---|
| фрагментная стоимость террейна @1600×900 | — | **0.16–0.30 мс** | 2.0 |
| `gpu-render` p50 (таймстемп three) | 0.06 | 0.08 | 2.0 |
| draw calls | 2 | 2 | — |
| треугольники | 293k | 293k | — |
| GPU-память (учтённая) | 85 МБ | **139 МБ** | 160 |
| vitest | 34 | 34 | — |
| ошибок в консоли | 0 | 0 | 0 |

Разбивка памяти: пул тайлов 113.8 МБ (было 81.3 — добавились два `rgba8`
splat-слоя, +34 МБ), процедурные PBR-массивы 22.4 МБ (2 × 512² × 8 × 4 Б ×
4/3 на мипы), скретч эрозии 2.5 МБ.

Разница между конфигурациями (при 3200×1800, средняя по 1200 кадрам):
`full` 5.01, `no stochastic` 4.74, `no triplanar` 4.96, `no parallax` 5.12,
`all four off` 4.55. То есть все четыре техники вместе — около 0.46 мс при 4×
пикселей, ≈ 0.12 мс при 1600×900. Разброс между отдельными конфигурациями
(0.03 мс при 1×) **ниже уровня шума** этой машины, и я не буду делать вид, что
померил вклад каждой по отдельности.

### Про доверие к числам

Абсолютное время кадра на этой машине не воспроизводится: за один вечер
идентичная работа при одинаковой камере давала `frame p50` от 0.20 до 5.00 мс
между прогонами. Поэтому одиночный `p50` здесь бесполезен как инструмент, и
0.16–0.30 мс получены **разностью** двух измерений в одном прогоне, где всё, кроме
числа пикселей, одинаково. Таймстемп three (`gpu-render` 0.08 мс) даёт тот же
порядок величины другим способом; я не верю ни одному из них точнее чем в разы,
но оба на порядок внутри бюджета 2.0 мс. Два независимых прогона разностного
метода дали 0.20 и 0.16 мс для полного материала; отдельные конфигурации во
втором прогоне легли в 0.29–0.30 мс. Разброс между прогонами — примерно вдвое,
и это честный предел точности здесь.

### Что было сломано и как чинилось (только то, что реально ломалось)

1. **Чёрный мир.** `StorageArrayTexture` наследуется от `Texture`, а не от
   `StorageTexture`, и поэтому у него **нет поля `mipmapsAutoUpdate`**. Код
   three в `Bindings.js` включает автогенерацию мипов по условию
   `isStorageTexture && mipmapsAutoUpdate === true` — то есть цепочка мипов
   выделялась и никогда не заполнялась. Уровень 0 был правильным, а всё, что
   дальше пары метров от камеры, читало неинициализированную память и рисовало
   чёрным. Лечится одной строкой, но найти это по картинке невозможно — нужно
   было лезть в исходники бэкенда.
2. **`textureLoad` по storage-текстуре с access `write`.** Второй проход
   генерации текстур читает то, что записал первый, из той же текстуры. Я завёл
   отдельный узел `storageTexture(tex)` для записи и `.toReadWrite()` для
   чтения — бэкенд свёл их в одну привязку и оставил `write`. Один узел на
   текстуру, read-write, и для загрузок, и для сохранений.
3. **Лимит storage-текстур.** Splat-ядру нужно пять (высота, поверхность, сток
   на чтение, два splat на запись), в WebGPU по умолчанию четыре. Адаптер
   поддерживает восемь; теперь запрашивается явно, с обрезанием по факту.
4. **`patch` — зарезервированное слово WGSL**, а `*` и `^` в одном выражении
   требуют скобок. Обе ошибки — компиляция шейдера, обе видны в консоли.
5. **Снега не было вообще.** Первая калибровка климата взяла 17 °C на уровне
   моря, что даёт нулевую изотерму на 2600 м. Я **померил** высоты вместо того,
   чтобы предполагать: развёртка ±60 км с шагом 1 км даёт максимум 902 м, а
   массив под горным ракурсом — 788 м при шаге 100 м. Мир низкий, значит мир
   холодный: 6 °C на уровне моря, линия 645 м весной. Это осознанно делает
   ландшафт приморско-субарктическим (Исландия, не Прованс) — луг, мох, осыпь,
   голая скала, топь, — а условие пустыни (T > 22 °C) недостижимо в принципе.
6. **Конфетти в средней дистанции.** Height-based смешивание по построению
   «победитель забирает всё», то есть превращает гладкое поле весов в двоичную
   мозаику с частотой карт высот. Вблизи это и нужно (трава между камнями),
   но на 500 м один пиксель накрывает несколько ячеек мозаики, и правильный
   ответ — их среднее. Окно смешивания теперь расширяется с расстоянием
   (0.14 → 1.6 на 120…600 м), что и есть вырождение в lerp, то есть в тот самый
   фильтр. Мипы тут не помогают: мозаика считается на пиксель, а не читается.
7. **Соты на скале.** Одна Worley-сеть даёт ячейки одного размера, а ячейки
   Worley в среднем шестиугольные — крупный план читался как правильная
   пчелиная сота на всю грань. Теперь два набора трещин: крупный «мастер» и
   мелкие поперечные, разрешённые только внутри части блоков.

### Решения, которые стоит знать

- **Снег — не запечённый вес.** ТЗ требует, чтобы линия ползла минутами; тайл
  печётся один раз и может прожить всю сессию. Поэтому в `splat1.a` лежит
  **восприимчивость** — насколько тексель удержит снег (уклон, вогнутость,
  подветренность, шум надувов), — а покрытие материал считает каждый кадр из
  живой линии. Побочный выигрыш: граница снега получается рваной и привязанной
  к рельефу, а не горизонтальным срезом по горе.
- **Из восьми биомов сэмплируются два.** Восемь материалов × два масштаба ×
  трёхтапное стохастическое смешивание — это больше сотни выборок на пиксель;
  так не делает никто. Веса на практике разрежены (мягкие полосы классификатора
  перекрывают два-три биома, никогда восемь), поэтому берутся два старших, а
  height-blend делает стык похожим на взаимопроникающие материалы, а не на
  двойной кроссфейд. Второй слой к тому же читается только при весе > 0.02 —
  это половина фрагментной стоимости шейдера.
- **Splat не морфится между уровнями LOD**, в отличие от высоты и нормали.
  Смешивать две классификации, снятые с разным разрешением, нельзя: они дальше
  идут в выбор «два старших» и в height-blend, и 50/50 двух разных рейтингов
  даёт не что-то среднее, а третий материал. Жёсткое переключение на границе
  уровня незаметно (поля биомов непрерывны в мировых координатах), смешивание
  рейтингов — нет.
- **Возмущение границы в мировых координатах.** Мотлинг splat-маски запечён в
  тайл, а тайл уровня 3 накрывает в восемь раз больше земли теми же 129²
  текселями — то есть с расстоянием мотлинг фильтруется и всё дальше ~50 м
  становится однородной заливкой одного биома (замерено на крупном плане:
  внятная пятнистость до 25 м, дальше ровная зелень). Height-blend теперь
  дополнительно смещается шумом 3.6 и 14 м, вычисленным **на пиксель из мировой
  позиции**, а значит одинаковым на всех уровнях. Оба члена гаснут, когда их
  период падает ниже нескольких пикселей.
- **Стохастический тайлинг включён на всех биомах**, а не только на траве и
  песке, как требует ТЗ. Причина — крупный план скалы: сеть трещин гранита
  повторялась каждые 2 м и читалась как решётка. Блок уже ограничен полосой
  9–1400 м и пропускается вне её, так что расширение стоит только там, где
  повтор иначе был бы виден.
- **Всё сэмплируется с явным уровнем мипа.** Несколько техник живут внутри
  `If`, а неявные производные под неоднородным потоком управления в WGSL —
  неопределённое поведение. Уровень считается один раз, вне всех ветвлений, из
  экранной производной мировой позиции.
- **AO уходит в `aoNode`, а не в цвет.** three применяет `aoNode` только к
  непрямому свету (правило 6 в CLAUDE.md); умножение в albedo затемнило бы и
  прямое солнце.

### Как выглядит

Смотрел PNG, все пять ракурсов, плюс режимы 10 и 11.

`p2-mtn.png` (горный ракурс, 1400×800). Снег лежит на гребне и в подветренных
карманах, ниже — палево-оливковые травяные склоны, в переходах песчано-серая
осыпь, на самом крутом — тёмная скала. Границы рваные, не по горизонтали и не
по контуру: снег заходит языками в желоба и обходит выпуклые рёбра. Никакой
повторяющейся сетки. Сцена всё ещё плоская по свету — это временное солнце с
полусферой, а не дефект материала; воздушная перспектива приедет в фазе 3.

`p2-high.png` (с 800 м, пологий взгляд). Главный критерий приёмки. Повторяющегося
паттерна нет — ни текстурной решётки, ни регулярных пятен. Земля читается как
мозаика луга, сухой травы и осыпи с органическими краями.

`p2-close.png` (2 м над землёй). Видны кочки травы с тенями между ними, пятна
подсохшего суглинка с полигональными трещинами усыхания, и трава языками входит
в эти пятна — это ровно то, ради чего height-based смешивание и делалось.
Слабое место: кочки Worley слегка «в горошек», регулярнее живой травы.

`p2-rock.png` (9 м над землёй, вершина). Снежная шапка с застругами и
асимметрией наветренный/подветренный, из-под неё выходят рёбра голой скалы с
блочной трещиноватостью двух масштабов. Растяжения текстуры на вертикалях нет —
триплан работает.

`bio-10.png` / `bio-11.png` (смесь и доминирующий биом). Широкие области с
рваными пятнистыми границами. Ни одной прямой линии, ни одного плавного
градиента через полкадра — то есть ни «резкой линии», ни «мыльного градиента»,
которых требует избежать ТЗ.

**Чего не видно и это плохо:** белые штрихи по швам тайлов — долг фазы 1b (284
сквозных пикселя, метод замера в разделе задачи 3). На тёмном грунте фазы 1 они
терялись, на светлом грунте фазы 2 читаются отчётливее. Я их не трогал: это
геометрия, а не материал.

### Компромиссы и долги

- `// TODO(quality):` **Швы фазы 1b стали заметнее.** Не новый дефект, но
  контраст с более светлым грунтом вырос. Кандидаты те же (юбки по краям
  патчей или затухание дельты эрозии к границе тайла), ни один не сделан.
- `// TODO(quality):` **Классификация зависит от прогресса эрозии.** Влажность
  берёт 0.35 от карты стока, а эрозия доходит до тайла за десятки секунд, так
  что в процессе стриминга эродированный тайл стоит рядом с неэродированным и
  классифицируется иначе — квадратное пятно на границе тайла (видно на
  `p2-nadir.png` из промежуточного прогона). Снизил вес с 0.55 до 0.35, пятно
  стало тональным сдвигом вместо смены биома, но не исчезло. Честное лечение —
  либо влажность, не зависящая от прогресса эрозии, либо отложенная
  классификация (ценой дырки в карте биомов на секунды).
- `// TODO(quality):` **Из восьми весов используются два.** Там, где реально
  сходятся три биома, третий отбрасывается, и на стыке возможно прямое ребро по
  линии, где второй и третий меняются рангом. Видно в режиме 11 в тройных
  точках луг/лес/осыпь; в затенённом рендере при нынешних весах не видно.
- `// TODO(quality):` **Стохастика — не полное гистограммосохраняющее
  преобразование Heitz & Neyret.** Реализована сетка и дисперсионно-сохраняющая
  рекомбинация; полный вариант требует LUT `T(input)→гаусс` и обратный на
  каждую текстуру. Контраст в зоне смешивания держится приблизительно, а не
  точно.
- `// TODO(quality):` **Лесная подстилка — это маска полога, а не подстилка.**
  Леса не будет до фазы 7; сейчас биом включается по низкочастотному шуму,
  температуре, влажности, уклону и высоте — по тем же входам, по которым фаза 7
  должна будет расставлять деревья. Если фаза 7 выведет свою маску отдельно,
  подстилка не совпадёт с деревьями, которые на ней стоят.
- `// TODO(quality):` **Ветер в надувах снега — константа** (с запада). Фаза 8
  даёт настоящее поле ветра, ориентация надувов должна пойти за ним.
- `// TODO(quality):` **Пустыня и пляжный песок почти не встречаются.** Условие
  пустыни (T > 22 °C) в 6-градусном мире недостижимо, пляж требует высоты < 7 м
  и малого уклона. Биом реализован и работает, но увидеть его можно только на
  побережье. Это следствие калибровки климата под высоту рельефа, а не ошибка.
- `// TODO(quality):` **Вклад отдельных техник не померен.** Разброс машины
  (0.03 мс при 1600×900 против шума в разы) не позволяет разделить триплан, POM,
  стохастику и макро. Общая фрагментная стоимость померена надёжно, разбивка —
  нет.
- `// TODO(quality):` **`MATERIAL_TEX_SIZE` не зависит от пресета качества.**
  512² × 8 × 2 массива — фиксированные 22.4 МБ на всех тирах, включая Low.
- `// TODO(quality):` **Ветка WebGL2 не получает ничего из фазы 2** — там нет
  compute, значит нет ни splat-маски, ни процедурных текстур; остаётся плоская
  раскраска по уклону из фазы 1. Проверить эту ветку на этой машине нечем.
- `// TODO(quality):` **KTX2/BasisU из ТЗ 2.3 не используется.** Текстуры
  генерируются в rgba8 на GPU; сжимать нечего и незачем, но если когда-нибудь
  появятся авторские наборы, тулчейна для них нет.
- `// TODO(quality):` **`SplatGenerator.EST_MS_PER_TILE` = 0.05 мс — оценка, а
  не замер.** Единственное непроверенное число в фазе. Используется только для
  того, чтобы списать с бюджета генерации то, что уже потратила классификация,
  так что ошибка делает эрозию чуть жаднее или чуть ленивее, но не ломает
  ничего. Метод разностного замера требует возможности зафиксировать темп
  классификации, которой у планировщика сейчас нет.

---

## Фаза 3 — Атмосфера и небо — РЕАЛИЗОВАНА

Это первая фаза, после которой картинка перестала быть «раскрашенным
рельефом». Весь свет в мире теперь выводится из атмосферы: солнце, луна и
ambient — три следствия одной модели рассеяния, ни одного подобранного цвета.

### Что сделано

**Цепочка LUT (ТЗ 3.1), Bruneton 2017 / Hillaire 2020, все четыре таблицы:**

| таблица | размер | частота | содержимое |
|---|---|---|---|
| transmittance | 256×64 rgba16f | один раз | пропускание точка→космос |
| multiple scattering | 32×32 rgba16f | один раз | 2-й и выше порядок, изотропно |
| sky view | 200×100 rgba16f | каждый кадр | небо в полярных координатах |
| aerial perspective | 32×32×32 rgba16f | каждый кадр | фроксель до 32 км |

Параметры — ровно табличные из ТЗ (радиусы 6360/6420 км, Рэлей
(5.802,13.558,33.1)e-6 м⁻¹ / 8 км, Ми 3.996e-6 + 4.4e-6 поглощение / 1.2 км,
g = 0.8, озон (0.650,1.881,0.085)e-6 тентом 25±15 км, альбедо земли 0.3).
Мутность (turbidity) — параметр, множитель плотности Ми
`pow(turbidity/2.5, 1.5)`: 1.5 → 0.46, 4 → 2.05, 8 → 5.79. Смена мутности
перепекает две статические таблицы; `setWeather('clear'|'haze'|'storm')` уже
работает и в фазе 8 к нему подключится погода.

**Аэроперспектива (ТЗ 3.2) — сдана.** Реализована через `scene.fogNode`:
three гоняет его на итоговом освещённом цвете каждого материала с `fog = true`,
то есть террейн получает её сейчас, а вода и растительность фаз 6–7 получат
даром. Каждый пиксель берёт `(экранный uv, расстояние)` из 3D-текстуры и
считает `color·transmittance + inscatter`. Срезы фрокселя квадратичны по
расстоянию (`d = ((i+0.5)/32)²·32 км`), потому что весь интересный набор
контраста происходит на первом километре. Есть переключатель `uAerial` — он же
инструмент A/B-замера и доказательства «какой артефакт чей».

**Солнце, луна, звёзды (ТЗ 3.3):**
- `math/Spherical.ts` — настоящая астрономия: юлианская дата, солнце по
  низкоточным формулам Астрономического ежегодника (±0.01°), луна по Мису
  (±0.3°), звёздное время с ставкой 360.98564736629°/сутки, перевод
  экваториальных координат в горизонтальные и в мировые. 15 unit-тестов
  (всего в проекте 49, все зелёные): склонение солнца проходит полный размах
  ±23.44° за год, полуденная высота на солнцестояниях равна 90−φ±23.44,
  матрица поворота небесной сферы ортонормальна и совпадает с поштучным
  пересчётом, луна проходит и новолуние и полнолуние за 30 суток и движется
  13°/сутки.
- `world/TimeOfDay.ts` — единственный источник правды по «когда». Площадка
  62° с.ш., 19° з.д. Снеговая линия теперь читает этот же час и сезон, а не
  интегрирует свои.
- Диск солнца 0.53° с потемнением к краю (Хестроффер–Маньян, край 0.30 от
  центра), нормировка по средней 1−u/3−v/2 чтобы потемнение перераспределяло
  свет, а не съедало его.
- Луна: фазы из настоящей элонгации, отражение по Ломмелю–Зелигеру (полная
  луна — плоский диск равномерной яркости, а не ламбертов шар), пепельный свет
  на тёмном лимбе, яркость по фотометрической формуле Миса
  (10^(−0.4(0.026α + 4e-9 α⁴))) — половинная луна выходит ~0.1 полной, а не 0.5.
- Звёзды: ~190 каталожных с настоящими координатами J2000 + 1800 процедурных
  (распределение N ∝ 10^0.6m, концентрация к галактической плоскости) —
  инстансированные квады, 1 draw call, гауссов профиль, мерцание через
  воздушную массу (у зенита почти нет, у горизонта сильное), ослабление той же
  таблицей пропускания. Млечный Путь — процедурный, печётся один раз в
  равнопромежуточную карту 512×256 в экваториальных координатах: два
  гауссиана по галактической широте, усиление к балджу в Стрельце, две шкалы
  пылевых полос. Вращается вокруг полюса вместе со звёздами (одна матрица 3×3).

**Экспозиция как у камеры (ТЗ 3.4):** физическая камера (диафрагма, выдержка,
ISO → EV100), всё в люксах и кд/м². Сцена рисуется в линейный HDR-таргет
rgba16f, гистограмма 256 бинов по log₂ яркости считается в compute по
фиксированной решётке 256×144 сэмплов, резолв берёт взвешенное среднее в
полосе 40–95 перцентиля, адаптация экспоненциальная с τ = 0.8 с на посветление
и 2.5 с на потемнение. Адаптированная экспозиция **не покидает GPU** — лежит в
storage-буфере, который читает квад тонмаппинга в том же сабмите. Эффект
Пуркинье применяется по палочковому отклику с двумя воротами: по яркости
пикселя (яркая луна остаётся белой) и по состоянию адаптации глаза (тени
солнечным днём не считаются скотопическими).

**`DebugScene` выпотрошен.** Ни `DirectionalLight`, ни `HemisphereLight`, ни
`scene.background` там больше нет. Ambient — девять SH-коэффициентов L2,
которые считает compute-проход проекцией sky-view LUT по 512 направлениям и
которые читаются обратно раз в 10 кадров в `THREE.LightProbe`. Правило 5
CLAUDE.md закрыто.

### Тайминги (WebGPU, Apple M-series, 1600×900, High, горный ракурс)

Метод — **чередующийся A/B**: тумблер переключается 6 раз, берётся медиана
парных разностей. Одиночные замеры на этой машине бесполезны (за один вечер
одна и та же работа давала 3.1…6.3 мс), а парные разности дрейф гасят.

| что | Δ медиана | бюджет | |
|---|---|---|---|
| **проход неба** (горный ракурс, ~40 % кадра) | **+0.10 мс** | 0.20 | ✅ |
| **проход неба** (камера в зенит, 100 % кадра) | **+0.10 мс** | 0.20 | ✅ |
| **LUT каждого кадра** (sky-view + фроксель + SH) | **+0.20 мс** | 0.30 | ✅ |
| **аэроперспектива** (fogNode) | **−0.10 мс** | — | ниже шума |
| **спрайты звёзд** (ночь, ~2000 инстансов) | **0.00 мс** | — | ниже шума |
| обратные чтения (SH + экспозиция) | +0.10 мс | — | |
| draw calls | 2 → 4 (небо, звёзды ночью 5) | — | |
| GPU-память | 139 → **151.6 МБ** | 160 | ⚠️ 8 МБ запаса |
| vitest | 34 → **49** ✅ | | |
| ошибок в консоли | 0 | 0 | ✅ |

Разрешение таймера кадра — 0.1 мс, поэтому «−0.10» у аэроперспективы читается
как «меньше одного деления», а не как ускорение.

**Полный кадр вырос примерно на 0.9 мс, и это замерено, а не оценено.**
В том же сеансе, на той же камере, сборка фазы 2 (git stash) дала p50
4.70/4.90/5.10/5.30 мс, сборка фазы 3 — 5.70/5.80/5.90/5.90 мс. Разница
складывается из прохода неба (0.1), LUT (0.2), обратных чтений (0.1) и
примерно 0.5 мс на HDR-таргет + гистограмму + квад тонмаппинга. Абсолютные
числа этой машине верить нельзя (в других прогонах того же вечера тот же ракурс
давал 3.1 мс), разности — можно.

Разбивка памяти фазы: HDR-таргет 1520×855×8 Б = 10.4 МБ, Млечный Путь
512×256 rgba16f = 1.0 МБ, четыре LUT вместе 0.55 МБ. Львиная доля прироста —
HDR-таргет, и он масштабируется с разрешением: на 4K это 66 МБ и бюджет 160 МБ
будет пробит. См. долги.

### Как выглядит

Смотрел PNG на семь моментов суток с одного гребня (14 апреля, 62° с.ш.).

`p3-5.png` (05:00, солнце 1.4°). Оранжево-розовая полоса вдоль всего горизонта,
переходящая через палевый в глубокий синий к зениту, без единой ступеньки.
Рельеф освещён тёплым скользящим светом, тени — отчётливо синие. Это ровно тот
кадр, ради которого фаза делалась.

`p3-8.png` / `p3-12.png` (22° и 37°). Голубое небо с правильным градиентом:
темнее к зениту, светлее и теплее к горизонту, у самого горизонта — сизая
дымка. Дальние гряды **заметно синеют и теряют контраст**: при выключенной
аэроперспективе (`/tmp/ap-off.png`) те же гряды зелёные и плоские, разница
видна невооружённым глазом. Критерий ТЗ выполнен.

`p3-17.png` (15.5°, золотой час). Освещённые склоны кремово-тёплые,
затенённые — голубовато-серые. Это и есть доказательство, что ambient берётся
из неба: ни одна константа в коде не окрашена.

`p3-19h5.png` (19:30, солнце −1.7°). Оранжевая полоса у горизонта, над ней
сиренево-синие сумерки, диск солнца уже за горизонтом (после того как я закрыл
дыру в выборке таблицы пропускания — см. ниже), рельеф — почти силуэт.

`p3-23.png` (23:00, солнце −17°, луна за горизонтом). Тёмно-синее небо с
свечением атмосферы у горизонта, звёзды, рельеф — чёрный силуэт. Ночь читается
как ночь.

`night-moonless.png` / `night-moonlit2.png` (камера в небо). Млечный Путь —
широкая полоса с ядром к Стрельцу и тёмными пылевыми полосами, сотни звёзд
разного цвета и яркости. При полной луне (2 января, луна 55°, 0.21 лк) небо
светлеет до тёмно-синего и звёзд остаётся заметно меньше — само собой, никакой
логики «выключить звёзды» нет.

Что не нравится честно, по убыванию:

1. **Треугольные полупрозрачные «плёнки» по всему рельефу.** Проверено
   `git stash`: они **есть и в сборке фазы 2** (`/tmp/old-mtn.png`), то есть
   это не долг этой фазы, а долг фазы 1b/2, который на новом свете стал
   виднее. Похоже на z-fighting между уровнями LOD (near = 0.1 при far =
   30000 даёт на 16 км шаг глубины порядка сотни метров).
2. **Кольцевые контуры вокруг луны** на ночном небе. Sky-view LUT — 200×100 на
   полную сферу, то есть 1.8° на тексель, а фазовая функция Ми при g = 0.8
   имеет пик заметно у́же. Билинейная интерполяция между текселями даёт видимые
   рёбра. Это ограничение метода, а не ошибка; лечится либо отдельным
   аналитическим членом для ореола, либо большей таблицей.
3. **Жёсткая кромка у клина сумеречного свечения** на кадре 02:30
   (`final-2h5.png`, солнце −12.9°). Само явление настоящее — это край тени
   Земли, спроецированной на атмосферу, тот же, что даёт «пояс Венеры». Но
   проверка затенения планетой в марше двоичная (`raySphere < 0`), а sky-view
   LUT даёт 1.8° на тексель, поэтому граница выходит резче, чем в жизни.
   Лечится мягким переходом по углу вместо шага.
4. **Дизер виден как зерно** на ночных кадрах. Он там нужен (8 бит на плавном
   градиенте — это полосы), но триангулярный шум ±1 LSB на почти чёрном фоне
   заметен. Фаза 10 должна заменить его синим шумом.

### Найдено и исправлено по ходу

1. **`wgslFn` не парсит ничего перед `fn`.** Регулярка three заякорена на
   `^fn`, поэтому блок констант перед объявлением ломал разбор молча — функция
   просто не собиралась. Константы переехали **внутрь** тела каждой функции
   (WGSL это позволяет, компилятор их сворачивает).
2. **`toneMapping()` возвращает vec4, а не vec3.** `ToneMappingNode` объявлен
   как vec3, но его `setup` строит `vec4(fn(color.rgb), color.a)`. `vec4(mapped, 1)`
   давал «Length of parameters exceeds maximum length of function 'vec4()'» без
   стека. Нашлось только включением `Node.captureStackTrace`.
3. **Таблица пропускания не определена ниже горизонта** — и это рисовало диск
   солнца на 1.7° **под** горизонтом на закатном кадре. Параметризация
   Брюнетона отображает uv в расстояние до верха атмосферы и про луч, который
   упирается в планету, не знает вообще. Добавлен горизонтный отсекатель,
   размазанный на 0.29° (один солнечный диаметр), чтобы солнце садилось, а не
   выключалось.
4. **Ночь метрилась в серый.** Отражённый экспонометр по построению выводит
   что угодно в средне-серое, поэтому безлунное небо 2.4e-4 кд/м² давало ровно
   ту же экранную яркость, что и полуденный луг 4000 кд/м². Это и есть «синий
   день» из ТЗ — не ошибка цвета, ошибка экспозиции. Добавлена компенсация
   `NIGHT_STOPS = 3` по кривой `smoothstep(6, −4, EV)`: на дневных уровнях ноль,
   к полной темноте −3 ступени.
5. **Красные звёзды рисовались чёрными точками.** Сдвиг Пуркинье применяется к
   **скомпонованному** кадру, а в учебничной скотопической подгонке
   коэффициент при красном отрицательный (палочки к глубокому красному
   действительно слепы) — поэтому красная звезда поверх слабо светящегося неба
   давала палочковый отклик **ниже** окружающего фона. Физически защитимо,
   визуально баг. Веса заменены на неотрицательные (0.02, 0.72, 0.42).
6. **Сдвиг Пуркинье гасил кадр в 2.5 раза.** Коэффициент подобран так, чтобы
   Rec.709-яркость на выходе совпадала со входной: преобразование меняет тон и
   насыщенность, а не экспозицию.
7. **190 каталожных звёзд — это пустое небо.** Замерено: одна звезда на 220
   квадратных градусов, в поле 60° их около дюжины. Добавлены 1800
   процедурных по настоящему распределению по величинам.

### Решения, которые стоит знать

- **Солнце и луна интегрируются в одном марше**, а не переключаются по порогу
  сумерек. Стоит около +40 % на шаг (плотности общие, удваиваются только
  фазовые функции и выборка пропускания) и даёт то, что ТЗ называет «плавный
  переход без единого скачка»: вклад луны становится больше вклада солнца сам
  собой около −8° высоты солнца, без единой строчки кода про это.
- **Sky-view LUT параметризован полным азимутом**, а не половиной с
  зеркальной симметрией, как у Илэра. Симметрия верна только когда источник
  один; у нас их два и они почти никогда не в одной вертикальной плоскости.
- **Единицы — километры.** Радиус планеты 6360 км, а высота сэмпла — это
  `length(p) − Rg`, вычитание почти равных чисел. В метрах f32 разрешает там
  полметра, в километрах — полмиллиметра.
- **Небо — полноэкранный треугольник с depth-test, рисуемый последним среди
  непрозрачных.** z = w, то есть глубина ровно 1.0, и LEqual выкидывает его
  везде, где хоть что-то нарисовано: на горном ракурсе шейдер неба не
  выполняется на 60 % кадра. `scene.background` выполнялся бы на всех 100 %.
- **Свечение атмосферы (airglow) добавлено в sky-view LUT**, а не в шейдер
  неба: тогда его подхватывает и SH-проекция, то есть безлунной ночью рельеф
  освещён тем же, чем светится небо.
- **Яркость поверхности лунного диска постоянна по фазе.** Серп не темнее
  полной луны на единицу площади — его просто меньше. Меняется освещённость от
  луны, а не яркость диска.
- **Экспозиция живёт на GPU.** Резолв пишет её в storage-буфер, квад
  тонмаппинга читает его в том же сабмите. Обратное чтение на CPU есть, но
  только ради строчки в F3.

### Компромиссы и долги

- `// TODO(quality):` **HDR-таргет масштабируется с разрешением и съест
  бюджет.** 1520×855 — это 10.4 МБ, 4K — 66 МБ при потолке 160. Плюс three
  вставляет **свой** проход преобразования цветового пространства после нашего
  квада (мы отдаём линейный цвет, он кодирует в sRGB) — это лишняя
  полноэкранная копия и ещё один rgba16f того же размера. Лечится переносом
  кодирования в наш квад и `outputColorSpace = Linear`, но это глобальная
  правка, которую логично делать один раз в фазе 10 вместе со всем post-стеком.
- `// TODO(quality):` **Кольца вокруг луны** — разрешение sky-view LUT против
  ширины пика Ми. См. «как выглядит», п. 2.
- `// TODO(quality):` **Каталог звёзд записан по памяти, а не импортирован из
  BSC5.** Первые несколько десятков — с точностью до угловой минуты, дальняя
  половина списка — до долей градуса. Ниже разрешения, с которым спрайты
  рисуются, но это не каталожная точность.
- `// TODO(quality):` **Рефракция не моделируется.** У горизонта она смещает
  диск на его собственный диаметр, то есть солнце садится примерно на две
  минуты раньше, чем следовало бы.
- `// TODO(quality):` **Планета не окклюдирует диски.** Солнце и луна гаснут по
  отсекателю в выборке пропускания, а не потому, что их закрыл шар. На
  сверхдлинном ракурсе над морем это может быть заметно.
- `// TODO(quality):` **Ветка WebGL2 не получает из фазы 3 ничего.** Без
  compute нет ни LUT, ни фрокселя; там остался жёстко заданный ключевой свет с
  полусферой, честно предупреждающий в консоль.
- `// TODO(quality):` **`NIGHT_STOPS`, `minEV` и `exposureBias` подобраны на
  глаз** и завязаны на подъём теней у AgX. Принципиальная версия — скотопическая
  кривая отклика **до** тонмаппинга, а не смещение экспонометра. Фаза 10.
- `// TODO(quality):` **Дизер — синусный хеш, а не синий шум.** Виден как зерно
  на ночных кадрах.
- `// TODO(quality):` **Пул таймстемпов COMPUTE переполняется** после долгого
  прогона: «Maximum number of queries exceeded». Фаза 3 добавила пять
  compute-проходов на кадр, а `Engine` резолвит раз в 15 кадров. Предупреждение,
  не ошибка, но счётчикам GPU после него верить нельзя.
- `// TODO(quality):` **Тени не отбрасываются** — фаза 4. Пока солнце светит
  сквозь горы, и на закатных кадрах это заметно: долина, которая должна быть в
  тени гребня, освещена.
- `// TODO(quality):` **Треугольные плёнки на рельефе** — долг фаз 1b/2,
  подтверждено `git stash`. Не трогал.
- Многократное рассеяние не затеняется планетой (приближение Илэра). Именно
  оно держит небо освещённым в минуты после заката; без этого допущения
  сумерки получаются на порядок темнее правильного.
